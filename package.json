{
    "private": true,
    "dependencies": {
        "jscs": "^2.2.1"
    },
    "scripts": {
        "lint": "jscs ./src",
        "compile": "babel src --out-dir dist",
        "start": "npm run-script compile && node index.js"
    }
}
